<div class="row">
    <div class="col">
        <div class="container d-flex justify-content-start">
            <form class="nav col-12 col-md-auto flex-fill mb-2 justify-content-center mb-md-0" role="search"
                method="POST" action="/search">
                <input type="search" name="searchTerm" class="form-control form-control-dark w-100 rounded"
                    placeholder="Search..." aria-label="Search">
            </form>
        </div>
    </div>
</div>

<section class="container">
    <table class="table mt-5">
        <thead>
            <tr colspan="2"></tr>
            <tr>
                <th scope="col">Term</th>
                <th scope="col">Definition</th>
                <th scope="col">
                    <a data-bs-toggle="dropdown" aria-expanded="false">
                        Category <i class="bi bi-caret-down"></i>
                    </a>
                    <ul class="dropdown-menu m-1 p-2">
                        <!-- Buttons to Filter Terms -->
                        <li id="filterRoot"><a>Term with no root</a></li>
                        <li id="filterPrefix"><a>Term with no prefix</a></li>
                        <li id="filterAnatomyPhysio"><a>Terms related to anatomy and physiology</a></li>
                        <li id="showAll"><a>Show All</a></li>
                    </ul>
                </th>
                <th>
                    <div class="btn-group me-2">
                        <a href="/add" class="btn btn-sm btn-outline-primary">+</a>
                    </div>
                </th>
            </tr>
        </thead>

        <tbody>
            <% medicalData.forEach(term=> { %>
                <tr id="termsList">
                    <td data-term="<%= term.category %>">
                        <%= term.term %>
                    </td>
                    <td data-term="<%= term.category %>">
                        <%= term.definition %>
                    </td>
                    <td data-term="<%= term.category %>">
                        <%= term.category %>
                    </td>
                    <td data-term="<%= term.category %>">
                        <div class="dropdown">
                            <a data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="bi bi-three-dots-vertical"></i>
                            </a>
                            <ul class="dropdown-menu m-1 p-2">
                                <li><a class="btn btn-link text-dark text-start" href="/view/<%= term._id %>">View More
                                        Details</a></li>
                                <li><a class="btn btn-link text-dark text-start" href="/edit/<%= term._id %>">Edit</i>
                                </li>
                                <form action="/edit/<%= term._id %>?_method=DELETE" method="POST">
                                    <button type="submit" class="btn btn-link text-dark text-start">Delete</button>
                                </form>

                            </ul>
                        </div>
                    </td>
                </tr>
                <% }) %>
        </tbody>


        <!--         <tbody id="">
            <% for( let index=0; index < medicalData.length; index++ ) { %>
                <tr>
                    <td>
                        <%= medicalData[index].term %>
                    </td>
                    <td>
                        <%= medicalData[index].definition %>
                    </td>
                    <td data-term="<%= medicalData[index].category %>">
                        <%= medicalData[index].category %>
                    </td>
                    <td>

                        <div class="dropdown">
                            <a data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="bi bi-three-dots-vertical"></i>
                            </a>
                            <ul class="dropdown-menu m-1 p-2">
                                <li><a class="text-dark" href="/view/<%= medicalData[index]._id %>">View More
                                        Details</a></li>
                                <li><a class="text-dark" href="/view/<%= medicalData[index]._id %>">Edit</i>
                                </li>
                                <li>
                                    <form action="/edit/<%= medicalData[index]._id %>?_method=DELETE" method="POST"
                                        class="position-relative">
                                        <a class="text-dark" type="submit">
                                            Delete
                                        </a>
                                    </form>
                                </li>
                            </ul>
                        </div>
                    </td>
                </tr>
                <% } %>
        </tbody> -->
    </table>
</section>